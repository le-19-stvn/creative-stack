Write the `CLAUDE.md` for this project.

It's Ledgerline — month-end close software for bookkeepers at small French accounting firms. Type: app. Stack is decided: TypeScript, Postgres, deployed on Fly. Conventions we settled on: no business logic in route handlers, tests assert behaviour not implementation. Hard constraint: the first firm onboards in six weeks.

At the project root right now I have `TASTE.md` and `BRAND.md`, both filled in.

There's no `POSITIONING.md` and no `STACK.md` yet — marketing isn't mounted, and the stack decisions above have only ever been said out loud, never written to a file.
